<!--index.wxml-->
<view>
  <header-cmp />
  <create-cmp class="create" wx:if="{{tab === 'create'}}" />
  <launched-cmp class="launched" wx:elif="{{tab === 'launched'}}" />
  <involved-cmp class="involved" wx:else />
</view>

<tabbar-cmp class="tabbar" bind:selectTab="changePage" />

