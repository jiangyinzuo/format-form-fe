import { config } from '/miniprogram/config.js'

class HTTP {
  request(params) {
    /*
     * @params.url
     * @params.data
     * @params.method: [GET, POST, PUT]
     */
    wx.request({
      url: '',
    })
  }
}